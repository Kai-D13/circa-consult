# QA report – 2026-07-12

## Automated PASS

- JavaScript syntax: `core.js`, background, content, popup, options và Admin Portal.
- 7/7 unit tests pass.
- Parse DOM label thật: `7266 - Paracetamol...` → product ID `7266`.
- Dataset validation chấp nhận exact IDs hợp lệ.
- Dataset validation từ chối duplicate pair, invalid ID và self-suggestion.
- Product `13720`, đúng SALES location, quantity `1`, có price → available.
- Product `2001395`, `stock_details = []`, có price → out of stock.
- Stock ở location khác bị loại.
- Có stock nhưng không có giá hợp lệ bị loại.
- Manifest JSON hợp lệ và package v1.2.0 tạo thành công.

## Supabase production PASS

- CLI linked đúng project `circa-consult`.
- Migrations được áp dụng thành công.
- Admin Auth confirmed và password login thành công.
- Admin RLS đọc được dataset versions/audit.
- File sample import thành công: 6 rules.
- Draft được tạo và publish thành công.
- Public RPC trả schema version 1, checksum và 6 effective rules.
- Audit ghi `create_draft` và `publish` đúng Admin actor.
- Chỉ dataset published được public RPC trả về.

Dataset production tại thời điểm test:

```text
version: 2026.07.12.063220.110
row_count: 6
source IDs: 85270, 174032, 2010773
suggested IDs: 59713, 75904, 84664, 85954, 2001395, 2008341
```

## Manual QA còn phải thực hiện trên POS

- Load unpacked extension trong Chrome thật.
- Xác nhận options nhận dataset version production.
- Add source product `85270`, `174032` và `2010773` vào cart.
- Đối chiếu suggestion với dataset.
- Xác nhận `2001395` không hiển thị vì hết tồn tại POS test.
- Đối chiếu các suggested IDs còn lại với tồn thực tế.
- Kiểm tra close/minimize, xóa source, chuyển đơn và thanh toán.
- Pilot trên 3 POS có sales location khác nhau.

Không đánh dấu các case manual là PASS cho đến khi chạy trên Chrome/POS thực tế.

## Giới hạn môi trường QA

- Docker Desktop không cài; Supabase CLI vẫn push migration remote thành công nhưng không cache local pg-delta catalog.
- In-app browser không có session khả dụng, nên visual Admin Portal cần kiểm tra thủ công theo `QA_CHECKLIST.md`.

